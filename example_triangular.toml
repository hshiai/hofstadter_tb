name = "triangular"

a1 = [1.0, 0.0]
a2 = [0.5, 0.8660254037844386]

# One orbital at the Bravais-lattice point.
# tau[alpha] = [xi_1^alpha, xi_2^alpha]
tau = [
  [0.0, 0.0]
]

# Nearest-neighbor hopping t = 1 along
# +/-a1, +/-a2, and +/-(a2-a1).
# h(alpha1, alpha2, ell) = <0, alpha1 | H | ell, alpha2>
# j  alpha1  alpha2  ell1  ell2  Re(t)  Im(t)
hopping = '''
0   0   0    1   0   -1.0   0.0
1   0   0    0   1   -1.0   0.0
2   0   0   -1   1   -1.0   0.0
3   0   0   -1   0   -1.0   0.0
4   0   0    0  -1   -1.0   0.0
5   0   0    1  -1   -1.0   0.0
'''

# k = k1*b1 + k2*b2
# label  k1  k2
band_path = '''
G   0.0                 0.0
K   0.6666666666666666  0.3333333333333333
M   0.5                 0.5
G   0.0                 0.0
'''

n_k = 100


[hofstadter]

# Choose "band" or "spectrum".
mode = "band"


[hofstadter.band]

# Phi/Phi0 = p/q; p may be negative and q must be positive.
p = 1
q = 7

# k = k1*P1 + k2*P2, where P1 = b1/q and P2 = b2
k_path = '''
0.2  0.0
0.2  1.0
1.0  1.0
'''

n_k = 80


[hofstadter.spectrum]

flux_min = -0.1
flux_max = 1.0
q_max = 67

# Minimum mesh for large q. Small q uses the denser adaptive mesh below.
k_mesh = [3, 3]
k_mesh_q1 = [30, 30]
